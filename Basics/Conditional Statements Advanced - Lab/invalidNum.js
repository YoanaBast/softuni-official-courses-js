function solve(num) {
let isvalid = true;

if (num === 0 || num >= 100 && num <= 200) {
    isvalid;
} else {
    isvalid =  false;
}

if (isvalid === false) {
    console.log('invalid');
}

}

solve(75)