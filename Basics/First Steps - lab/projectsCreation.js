function projectCreation(name, count) {
    let duration = 3;
    let totalTime = duration * count;
    console.log(`The architect ${name} will need ${totalTime} hours to complete ${count} project/s.`)
}

projectCreation("George", 4)